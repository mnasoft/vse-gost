* Создание сервиса
#+begin_src lisp
  (asdf:load-system "vsegost/web")

  (sb-ext:save-lisp-and-die "vsegost-web.exe"
                            :executable t
                            :compression t)
#+end_src

Запуск standalone бинарника:
#+begin_src sh
  vsegost-web.exe --eval "(vse-gost/web:start-gosts)"
#+end_src

Создание и установка systemd-сервиса
#+begin_src sh
  # Рекомендуемый вариант через общий установщик:
  ./INSTALL.run install
  ./INSTALL.run service-install
  ./INSTALL.run service-status

  # Локальный вариант из каталога скриптов:
  cd src/web/sh
  chmod +x vsegost-web-tmux.sh mk-service.sh
  ./mk-service.sh

  sudo systemctl start vsegost-web
  systemctl status vsegost-web

  # Остановить
  sudo systemctl stop vsegost-web
#+end_src

#+RESULTS:


